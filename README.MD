#next-dom-data

##description:
+ Dom data module based on next toolkit.

##usage:
+ nx.$(el).data('name','fei')
+ nx.$(el).data('name') //fei
+ multi:
```javascript
  nx.$(el).data({
    name:'fei',
    hobby:'programming'
  });
```
